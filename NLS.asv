function [ est ] = NLS( dist, anchors )
%NLS Summary of this function goes here
%   Detailed explanation goes here
bx = 6;
by = 6;

while true
    f1 = sqrt((bx-anchors(1,1)).^2+(by-anchors(1,2)));
    f2 = sqrt((bx-anchors(2,1)).^2+(by-anchors(2,2)));
    f3 = sqrt((bx-anchors(3,1)).^2+(by-anchors(3,2)));
    f4 = sqrt((bx-anchors(4,1)).^2+(by-anchors(4,2)));

    f=[f1;f2;f3;f4];
    
    J = Jacobian(bx,by,anchors);
    
    linsolve(J.*J',)
end



end

