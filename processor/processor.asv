client = tcpclient('localhost', 9100);
while true
    data = read(client);
    if data
        data
        isRSSI = true;
        mult = 1;
        id = 0;
        rssi = 0;
        for i= length(data):-1:1
            if data(i)==59
                isRSSI = false;
                continue;
            end
            if isRSSI
                rssi+= (data(i))*mult
            end
        end
    end
end
